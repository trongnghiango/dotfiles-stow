# ~/.zshenv
# Đọc bởi MỌI zsh instance (login, non-login, interactive, non-interactive).
# Chỉ làm 1 việc: chỉ định ZDOTDIR để zsh tìm config đúng chỗ.
# Không đặt bất cứ thứ gì khác ở đây.
export ZDOTDIR="${HOME}/.config/zsh"
